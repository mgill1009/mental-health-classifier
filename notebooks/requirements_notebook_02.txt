# ── Notebook 02: Baseline Model (TF-IDF + Logistic Regression) ───────────
# Run on: local CPU or any cloud notebook (no GPU needed)
# Install: pip install -r requirements_notebook_02.txt
# Note: includes all notebook 01 deps — safe to use as a superset

# ── Core data ─────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── Visualisation ─────────────────────────────────────────────────────────
matplotlib==3.9.4
seaborn==0.13.2

# ── NLP / ML ──────────────────────────────────────────────────────────────
scikit-learn==1.8.0          # TfidfVectorizer, LogisticRegression, metrics
nltk==3.9.1
joblib==1.4.2                # model serialisation (pipeline.pkl)

# ── Notebook environment ──────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.29.5
ipywidgets==8.1.5
tqdm==4.67.1
